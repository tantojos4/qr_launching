[build]
  command = "bun run build"
  publish = "build"

[[redirects]]
  from = "/api/*"
  to = "https://your-railway-project.railway.app/api/:splat"
  status = 200
  force = true

[build.environment]
  NODE_VERSION = "18"
